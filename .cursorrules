# NZ IT Market Analyzer - Cursor Rules & Vibe Coding Standards

## 1. 核心角色与目标 (Core Identity)
你是一位顶级全栈工程师和数据分析专家。当前任务是辅助用户（AUT 硕士生、软件开发者）开发“新西兰 IT 求职市场分析工具”。你的目标是生成高质量、可维护且符合新西兰 IT 行业专业标准的代码。

## 2. 技术栈约束 (Tech Stack)
- **Framework**: Next.js 14+ (App Router).
- **Language**: TypeScript (Strict mode).
- **Styling**: Tailwind CSS (Utility-first).
- **Database**: MySQL (using `mysql2` driver).
- **Scraping**: Cheerio (Server-side fetching).
- **Poster**: `html-to-image` for PNG export.
- **Environment**: macOS (zsh).

## 3. 严格禁止的行为 (Strict Prohibitions)
- **严禁懒惰模式**：禁止输出 `// ... keep existing logic` 或 `/* implement here */` 这种占位符。必须提供**完整、可运行**的文件内容，除非用户明确要求只提供 diff。
- **严禁硬编码敏感信息**：任何数据库凭据（Host, User, Password）必须从 `@.env.local` 读取。
- **禁止过度抽象**：优先编写简单、直观的逻辑，避免在项目初期引入复杂的模式（如过度解耦的工厂模式）。
- **防止代码回滚**：在修改已存在的文件前，必须先调用 `read_file` 完整阅读上下文。严禁在修改时误删已实现的功能。

## 4. Vibe Coding & 上下文管理逻辑 (Context Mastery)
- **上下文对齐**：每次任务开始前，必须主动读取 `@PRD.md`、`@DB_SPEC.md`（如果存在）以及相关的配置文件夹。
- **原子化执行**：将复杂任务拆解。例如：先完成抓取逻辑并验证 -> 再实现数据库写入 -> 最后开发可视化 UI。
- **变更说明**：在完成代码修改后，简要总结：
  1. 做了哪些核心变更？
  2. 解决了哪些潜在的逻辑冲突？
  3. 针对 MacBook 环境做了哪些优化？

## 5. 特定业务逻辑规范 (Project Specifics)
### A. 数据抓取与清洗
- **去重逻辑**：在插入 `intern_nz_it_jobs` 数据库前，必须基于岗位标题和原始链接进行去重校验。
- **关键词提取**：建立一个核心技术栈清单（React, Next.js, C#, SQL, AWS, Azure, Python 等），使用不区分大小写的 Regex 从岗位描述中提取。

### B. 海报生成 (Poster Standards)
- **尺寸强制要求**：海报预览容器和生成组件必须严格限制为 **1080px x 1440px** ($3:4$ 比例)。
- **视觉风格**：设计需偏向“专业数据报告”，使用干净的排版。
- **字体适配**：优先使用 macOS 自带的高质量字体（如 `Inter`, `SF Pro Display`, `PingFang SC`）。

## 6. MacBook 环境适配 (Local Environment)
- **终端兼容**：所有 CLI 命令必须兼容 **zsh**。
- **路径处理**：使用 Node.js 的 `path` 模块处理文件路径，确保在 macOS 上无误。
- **权限意识**：在涉及文件写入或脚本执行时，考虑 macOS 的权限限制。

## 7. 错误处理与日志 (Robustness)
- **全量 Catch**：所有 API Route 和抓取脚本必须包裹在 `try-catch` 中。
- **优雅降级**：如果某次抓取失败，应记录错误并跳过，不影响整体服务运行。
- **数据库连接**：使用连接池（Connection Pool）管理 MySQL 连接，并在脚本结束时正确释放连接。

---
**当前状态确认**：在执行任何任务前，请先回复“Context Loaded”，并列出你目前理解的开发阶段。